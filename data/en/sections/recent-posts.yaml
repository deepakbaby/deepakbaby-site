# section information
section:
  name: Blog
  id: posts
  enable: true
  weight: 30
  showOnNavbar: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
