import 'popper.js'
import 'bootstrap'
import '@fortawesome/fontawesome-free/js/all'
import feather from 'feather-icons'

// Minimal application.js to bypass build errors
// We can add back features/sections if needed
console.log('Custom application.js loaded');

feather.replace();
